<% for order in  @orders %>
  <p>Name: <%= order.pretty_name() %></p>
  <p>Order: <%= order.quantity %> X <%= order.topping %> </p>
  <p><a href='/pizza-orders/<%= order.id %>'>Show Order</a></p>
  <form method='post' action='/pizza-orders/<%= order.id %>/delete'>
    <input type ='submit' value='Delete Order'>
  </form>
  <p><a href='/pizza-orders/<%=order.id%>/edit'>Edit Order</a></p>
  <% end %>

<!--No equals sign in orders because we don't want to render this on the page. <%= would actually cause the element to be displayed. -->
